[project]
name = "myuni"
version = "0.1.0"
description = "A CLI tool for importing student grades from CSV into university Excel grade sheets"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
]
